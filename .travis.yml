language: python
python:
    - "2.6"
    - "2.7"
    - "pypy"
install:
    - pip install -r requirements.txt --use-mirrors
script:
    - nosetests --with-coverage --cover-package=pyconza2014
    - flake8 setup.py pyconza2014 tests
after_success:
  - coveralls